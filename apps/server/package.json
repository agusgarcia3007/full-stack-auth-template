{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bunx tsc --skipLibCheck --noEmit && bun build src/index.ts --target=bun --outdir=dist --packages=external",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.5",
    "hono": "^4.9.9",
    "hono-rate-limiter": "^0.4.2",
    "postgres": "^3.4.7",
    "resend": "^6.1.2",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.5"
  }
}
